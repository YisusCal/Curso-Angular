/* Aqui se creas la interpolacion, es decir a la interfaz Persona se le asignas dos tipos de datos, los cuales se deben respetan dependiendo los datos ingresados en la clase exportada. */
export interface Persona {
  nombre: string;
  edad: number;
}
